import 'package:flutter/material.dart';
import 'package:flutter_todo_provider_app/widgets/task_tiles.dart';
class TasksList extends StatelessWidget {
  const TasksList({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [
        // TaskTileState(),
        // TaskTileState(),
        // TaskTileState(),
      ],
    );
  }
}